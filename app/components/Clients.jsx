var React = require('react');

export var Clients = React.createClass({
  render: function () {
    return (
      <div>
        Client component
      </div>
    )
  }
});

module.exports = Clients;
